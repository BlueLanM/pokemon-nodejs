# Railway 部署环境变量配置示例
# 将这些变量在 Railway 仪表板中配置

# 服务器端口（Railway 会自动提供）
PORT=3000

# 数据库配置（使用 Railway MySQL）
# Railway 会自动提供以下变量，你也可以手动设置：
# MYSQLHOST=containers-us-west-xxx.railway.app
# MYSQLPORT=3306
# MYSQLUSER=root
# MYSQLPASSWORD=your_password
# MYSQLDATABASE=railway

# 或者使用 Railway 提供的 DATABASE_URL（如果使用）
# DATABASE_URL=mysql://user:password@host:port/database

# CORS 允许的源（前端 GitHub Pages 地址）
# CORS_ORIGIN=https://your-username.github.io

# Node 环境
NODE_ENV=production
